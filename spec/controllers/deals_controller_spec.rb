require 'spec_helper'

describe DealsController do
  it "directs to index with a notice on successful save" do
    # Deal.any_instance.stub(:valid?).and_return(true)
    # post "create"
    # assigns[:deal].should_not be_new_record
    # flash[:notice].should_not be_nil
    # response.should redirect_to(deals_path)
  end

  # it "shows form" do
  #   render "/deals/index.html"
  #   response.should have_tag('form[action=?][method=?]')
  # end
end